<main class="container">
  <div class="header">
    <%= link_to "Back to posts", root_path, class: "btn btn--primary", data: { turbo_prefetch: false }%>
  </div>

  <%= turbo_frame_tag @post do %>
    <%= render "form", post: @post %>
  <% end %>
</main>